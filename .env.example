# ── API ──────────────────────────────────────────────────
# Backend API base URL. Must match the backend PORT + prefix.
# Local dev: http://localhost:<PORT>/api/v1
# Production: /api/v1 (same-origin reverse proxy)
VITE_API_URL=http://localhost:3000/api/v1
